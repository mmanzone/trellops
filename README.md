Proof of concept of a dashboard page showing the count of Trello cards in a List for each Trello board the user has access to.
Each dashboard is customisable, with the ability to choose which lists to display, the colour and layout. The dashboard automatically refresh every xx second (configurable).
The configuration for each dashboard can be exported and re-imported to share with other users
